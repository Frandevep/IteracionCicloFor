#include <stdio.h>

int main()
{
    int i=0;
    // Usamos un bucle for para contar del 1 al 5
    for (i=1; i<=5; i++) //verificamos si i <= 5, y aumentamos i en 1
    {
        printf("Contador: %d\n", i); // Mostramos el valor actual de i
    }

    return 0;
}
